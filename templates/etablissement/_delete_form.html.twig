<form method="post" action="{{ path('etablisement_delete', {'id': etablisement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etablisement.id) }}">
    <button class="btn">Delete</button>
</form>
