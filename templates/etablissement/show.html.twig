{% extends 'base.html.twig' %}

{% block title %}Etablisement{% endblock %}

{% block body %}
    <h1>Etablisement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etablisement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ etablisement.name }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ etablisement.adress }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ etablisement.city }}</td>
            </tr>
            <tr>
                <th>Postalcode</th>
                <td>{{ etablisement.postalcode }}</td>
            </tr>
            <tr>
                <th>Name_contact</th>
                <td>{{ etablisement.nameContact }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ etablisement.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ etablisement.email }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ etablisement.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etablisement_index') }}">back to list</a>

    <a href="{{ path('etablisement_edit', {'id': etablisement.id}) }}">edit</a>

    {{ include('etablissement/_delete_form.html.twig') }}
{% endblock %}
